name: Release Helper

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain local release flow
        run: |
          echo "Releasy se vytvareji lokalne pres release.bat / scripts/release_build.py."
          echo "Workflow zustava jen jako pomocna dokumentacni stopa v repozitari."
